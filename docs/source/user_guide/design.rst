Design
======

.. note::

   **WRITEME.** This page is a stub. Cover why a network is a graph rather
   than a module tree, how parameters and RNGs live as shared variables, the
   rewrite passes the library adds, and the consequences for interoperating
   with plain PyTensor and PyMC.

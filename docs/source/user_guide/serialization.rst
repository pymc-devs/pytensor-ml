Saving and loading
==================

.. note::

   **WRITEME.** This page is a stub. Cover weights-only checkpoints
   (``save_state`` / ``load_state``) versus full-network round-trips
   (``save_pretrained`` / ``from_pretrained``), the on-disk layout, and what
   graph serialization does and does not support.

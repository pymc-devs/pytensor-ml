Building networks
=================

.. note::

   **WRITEME.** This page is a stub. Cover what a layer is (a graph
   constructor, not a stateful module), naming and parameter ownership,
   composing with ``Sequential`` and the other combinators, and writing a
   custom layer.

Training
========

.. note::

   **WRITEME.** This page is a stub. Cover the training step as a compiled
   function, losses and target placeholders, batching with ``DataLoader``,
   stateful layers (batch norm statistics, dropout RNGs) and how their
   updates are threaded, and the train/predict graph split.

Optimizers and schedules
========================

.. note::

   **WRITEME.** This page is a stub. Cover update rules as compositions of
   transforms, ``chain``, gradient clipping and weight decay, learning-rate
   schedules and the training clock they read, and step guards
   (``skip_if``, ``apply_if_finite``).
